import { TicketsAll } from "../../../pages/tickets/part/TicketsAll"
import { Supervisor } from "../Supervisor"

export const TicketsSup = () => {
    return (
        <Supervisor>
            <TicketsAll />
        </Supervisor>
    )
}
