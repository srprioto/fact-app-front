import { Layout } from "../../components/layouts/Layout";
import { Usuarios } from "./part/Usuarios";

export const IndexUsuarios = () => {
    return (
        <Layout>
            <Usuarios />
        </Layout>
    )
};
