import { Layout } from "../../components/layouts/Layout"
import { TicketsAll } from "./part/TicketsAll"

export const IndexTickets = () => {
    return (
        <Layout>
            <TicketsAll />
        </Layout>
    )
}
